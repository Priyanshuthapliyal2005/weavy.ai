export const HISTORY_LIMIT = 50;

export const ERROR_DISPLAY_TIMEOUT_MS = 3000;

export const NODE_MIN_WIDTH = '383px';
export const NODE_MAX_WIDTH = '383px';

export const TEXTAREA_MIN_HEIGHT_LINES = 6;
export const TEXTAREA_MAX_HEIGHT_PX = 465;
export const TEXTAREA_LINE_HEIGHT = 1.5;
export const TEXTAREA_FONT_SIZE = 16;

export const IMAGE_DISPLAY_MIN_HEIGHT = 360;

export const DOWNLOAD_STAGGER_DELAY_MS = 100;

export const TOOLTIP_OFFSET_PX = 8;

export const ZOOM_DEFAULT_PERCENT = 100;

// Node creation and connection delays
export const NODE_CREATION_DELAY_MS = 0;
export const CONNECTION_TIMEOUT_MS = 1000;

// Image fetch timeouts and limits
export const IMAGE_FETCH_TIMEOUT_MS = 30000; // 30 seconds
export const MAX_IMAGE_SIZE_BYTES = 10 * 1024 * 1024; // 10MB

// API timeouts
export const LLM_API_TIMEOUT_MS = 60000; // 60 seconds
